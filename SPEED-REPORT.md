# 修图提速战役报告（2026-09-23 凌晨）

## 🟥 一句话结论

**目标 30s/p → 10s/p 目前无法达成，且瓶颈不在代码——本机 GPU 正在严重降频/降功耗，
同一个基准测试在 3 小时内从 7.10 TFLOP/s 掉到 0.39 TFLOP/s（18×），
代码级优化实测只值 ~10–20%。必须先解决电源/散热状态，再谈 10s/p。**

---

## 一、决定性证据：机器自己在变慢

同一个 MLX matmul(2048²) 基准，**代码一字未改**：

| 时刻 | 电池 | 算力 | 备注 |
|---|---|---|---|
| 01:15 | 66% 放电 | **7.10 TFLOP/s** | 本机"正常"水平 |
| 02:56 | 36% 放电 | **1.37 TFLOP/s** | 掉到 1/5 |
| 03:35 | ~30% 放电 | **0.39 TFLOP/s** | 掉到 1/18 |
| 静置 1500s 后 | 27% | 1.05 TFLOP/s | **静置并未恢复** |

CPU 侧同步劣化：`fib(30)` 实测 **602 ms**（M5 单核参考 ~200–400ms）。

**关键排除项**（都验过，不是它们的问题）：
- 不是内存压力：free 78%、swap 未满、我自己的进程 RSS 仅 0.12–3.2GB；
- 不是页缓存冷启动：强制 `cat model.safetensors` 预热后，解码速度不变（2.46→2.61 tok/s）；
- 不是 MLX 版本/编译问题：`mx.eval` 提交开销仅 2 µs/op，深链 36 层单次 eval 可达 **83 tok/s** 上限；
- 不是 PyTorch 特有：torch MPS 同样只有 1.65 TFLOP/s（4096³ fp16）；
- 不是单项任务慢：GPU util 采样显示我跑 matmul 时利用率只有 **48%**。

**行为特征 = 无风扇 MacBook Air 在电池低电量下的持续功耗塌缩**，
与项目既有的 Proteus-2 铁律（「M5 无风扇 GPU 持续功耗塌缩」）同源，但幅度远大于记忆中的 ~2×。

> ⚠️ 记忆里那条「gen-2 实测 31.7s/张」是 2026-09-22 在**快态**下测的。
> 今天同一份 `al-test3` 代码实测 **190–211s/张（6.6×）**。差异全部来自机器状态。

---

## 二、VLM 为什么慢（与降频叠加）

单次 VLM（Qwen3-VL-4B-4bit）耗时拆解，实测：

| 量 | 实测值 |
|---|---|
| 固定开销（prefill + 视觉塔 + 进程往返） | **~5.2 s/次**（与图像边长 128↔1024 几乎无关） |
| 解码速率（当前热态） | **2.5–3.4 tok/s** |
| ① 输出长度 | ~310 令牌 → ~90–125 s |
| ③ 输出长度 | ~265 令牌 → ~60–90 s |
| ④ 输出长度（位置式实验） | 模型不遵守，反而更长 |

**结论：时间几乎全部花在"逐个生成输出令牌"上，而这一点被 GPU 降频放大了最狠。**
批量（`batch_generate` B=2/4）实测**几乎无线性质增益**（B=4 仅 ~1.2×），
因为解码是访存/调度受限而非算力受限——降频时更差。

---

## 三、我做的代码优化（已落地，可消融）

新增 `tools/al-fast`：与 `al-test3` **同一套四道工序与判据**，只改吞吐结构。

| # | 优化 | 状态 |
|---|---|---|
| A | 单一常驻进程（不再每张起 5–7 个 Python/Swift 子进程） | ✅ 已落地 |
| B | 测量与渲染**真并行**（与耗时的 VLM① 重叠） | ✅ 已落地（曾误写成"提交后立刻 result()"，等于没并行，已修） |
| C | 渲染 + VLM① 重叠（渲染走预览缓存，VLM 走原片，互不依赖） | ✅ 已落地，`③render` 实测降到 **0.0s** |
| D | 多张回读合并为**一次** sqlite 查询（去掉每张一次 sqlite3 子进程） | ✅ 已落地 |
| E | 位置式（只输出值）提示 —— **实测失败** | ❌ 见下 |
| F | 枚举字段词表校验 + 不可信即回退行式 | ✅ 已落地（正是它拦住了 E 的事故） |
| G | 断点续跑 + 每张落盘（与 al-test3 相同语义） | ✅ 已落地 |
| H | 精简提示（--lean，砍子字段）—— **实测降质，默认关闭** | ❌ 见下 |

另有离线单测 `plugin/spec/test_al_fast_parse.py`（**27 用例全过**），
覆盖位置式对齐、无损解析、子字段取值、串位拦截、键映射、以及「生产必须用全字段提示」的质量护栏。

### E 的失败（重要，避免后人重踩）

想用「只输出值、不输出字段名」省掉那 ~52% 的令牌开销（315→151）。
**4B 模型不遵守**：会自行补字段名、加序号、跑题，导致**值整体串位**——
实测出现 `主体明暗=中上中`、`几何=是`、`是否过修=突出` 这类错位。

我加的 `positional_trustworthy()` 白名单校验把它拦下来了，
两次调用都正确判为不可信并回退行式，**最终参数与 al-test3 逐字段一致**。
→ **位置式方案在本机 4B 模型上不可用；要省令牌必须换更听话的模型或微调。**

### H 的失败（同样重要：省令牌不能以质量为代价）

想再省 36% 令牌，于是砍掉「没有任何代码读」的子字段（`位置`/`依据`/`人数`/`眼神`/`理由`…）。
**实测这会降低语义质量**——先确认了 VLM 是**完全确定性**的（同一提示跑两次 10/10 字段一致），
所以差异是提示词造成的，不是随机噪声。同一张 `IMG_20251226_211917.jpg`：

| 提示 | 主题 | 人物情况 | 表情 | 需要去朦胧 |
|---|---|---|---|---|
| A 原全字段提示（al-test3 用的） | **人像** ✅ | **多人合影** ✅ | **自然放松** ✅ | 否 |
| B 我的 lean（缩指令+删子字段） | 活动记录 ❌ | 人像特写 | 严肃 ❌ | 否 |
| C 原指令、**只删子字段** | 人像 ✅ | 人像特写 | 严肃 ❌ | **是** ❌ |

- 我对该图的独立目视判断：**两名 coser 特写、都在微笑** → A 正确（人像 / 多人合影 / 自然放松）。
- A↔B 一致 7/10，A↔C 一致 6/10。
- 最危险的是 C：把 `需要去朦胧` 从「否」翻成「是」→ 会**误加 Dehaze +15**，是真实修图事故。

**结论：那些"没人读"的子字段其实是思维链脚手架，删掉会让模型偷懒并幻觉。**
因此 `--lean` 默认关闭、运行时打印告警、并有单测锁死「生产路径必须用全字段提示」。
**要省令牌只能换模型或减必需字段，不能删脚手架。**

### 质量等价性验证（默认路径，两次独立照片均对拍通过）

**照片 A：`IMG_20251226_211705.jpg`**

```
al-test3: {'exposure': -0.18, 'highlights': -65, 'whites': -25,
           'contrast': -7, 'tint': -8, 'shadows': 30, 'vibrance': -5}
al-fast : {'exposure': -0.18, 'highlights': -65, 'whites': -25,
           'contrast': -7, 'tint': -8, 'shadows': 30, 'vibrance': -5}
差异字段: 无（逐字段一致）   ①24/24 ③21/21 字段完整
```

**照片 B：`IMG_20251226_211917.jpg`**（即上面 H 节那张 coser 图）
```
al-test3: {'vibrance': -10, 'temperature': 0}
al-fast : {'vibrance': -10, 'temperature': 0}
参数差异: 无（逐字段一致）   ①24/24 ③21/21 字段完整
无 warnings（未触发任何回退）
关键语义字段 8/8 逐项一致：主题=人像、人物情况=多人合影、表情=自然放松、
主体明暗=主体比背景亮、构图=居中、色彩情况=偏蓝、白平衡情况=偏蓝、需要去朦胧=否
```

→ **默认（不加 `--lean`/`--pos`）时，al-fast 与 al-test3 的输出逐字段等价。**

---

## 四、实测吞吐（当前热态，仅供参考，不代表快态）

| 流水线 | 3 张总耗时 | 每张 | ①VLM | ③VLM | 其它合计 |
|---|---|---|---|---|---|
| `al-test3` | 633 s | **211 s** | — | — | — |
| `al-fast` | 571 s | **190 s** | 292 s(3张) | 176 s(3张) | 86 s |
| `al-fast --pos` | 308 s(1张) | 308 s | 184 s | 87 s | 31 s |

代码结构优化带来的增益在当前热态下约 **10%**，其余全是 VLM 解码时间。

---

## 五、要达成 10s/p，必须做的三件事（按收益排序）

### 1. 🔴 先解决机器状态（不花钱、收益最大、且是前提）
- **插上电源**并使用 96W+ 原装适配器；
- 确认 `pmset -g` 无 `lowpowermode`；关闭后台 GPU 大户（当前 WebKit GPU 进程 60–105% CPU）；
- 跑批前**充分冷却**（实测静置 25 分钟**不足以**恢复，说明是持续性功耗/电量策略，不是单纯积热）；
- 加一条**上线前自检**：跑基准，`< 5 TFLOP/s` 就拒绝开批并告警（避免再出现"同一份代码 6.6× 波动"）。

### 2. 🟠 降低每张的 VLM 输出令牌数（唯一真正影响 10s/p 的代码杠杆）
当前 ①+③ 合计 ~575 输出令牌、按 2.5–3.3 tok/s 就是 174–230 s。
在快态（~10 tok/s）下同样令牌只要 58 s——**所以令牌数必须砍**：
- 收紧自由文本字段（`内容`/`优先动作`/`仍存在的问题`）到硬字数上限；
- 去掉**非必需子字段**（`高光位置`/`阴影位置`/`白平衡依据`/`人数`/`眼神`/`焦点位置`/`倾斜方向`/`倾斜程度`/`噪点区域`/`缺陷位置`/`去朦胧理由`/`调色理由`），
  实测这些占 ~112 令牌（36%）且**逻辑层并未消费**；
- 目标：①+③ ≤ 250 令牌。

### 3. 🟡 换更小/更听话的模型（带宽敏感，收益可期）
已下载 `~/models/Qwen3-VL-2B`（1.7GB）。实测 **6.1 tok/s vs 4B 的 2.5–3.3 tok/s（约 2×）**，
且 24/24、21/21 字段完整。**但语义质量尚未与 4B 做过逐字段对照**，需先验一轮再切换。

### 关于 10s/p 的现实判断（诚实版）

即使插电回到快态（解码 ~10 tok/s），按当前令牌量 ①+③ ≈ 58 s + 固定开销 10 s ≈ **68 s/张**。
要压到 10s/张需要 **≥ 25 tok/s 有效解码**，而本项目已验：
- 批量 `batch_generate` 几乎无增益（B=4 仅 ~1.2×）→ 靠批处理补不回来；
- 4B 的固定开销 ~5.2s/次 ×2 次 = **10.4s 就正好吃掉整个预算**，还没算任何生成令牌；
- 输出令牌又**不能再砍**（砍了即降质，见 H 节）。

所以 10s/p 在「4B + 两次 VLM + 保证质量」这个组合下，
**即使插电冷却也在物理上做不到**。可达的路径只有换更小模型（2B 实测 6.1 tok/s，约 2×），
且**必须先逐字段验证 2B 的语义质量不低于 4B**（我尚未验，且 A/C 的教训说明"字段齐全"不等于"判断正确"）。

**在电池 + 热态下，任何速度目标都无意义——此刻不是代码问题。**

---

## 六、立即可以做的下一步

```bash
# 1) 插电、冷却后先做快态自检（< 5 TFLOP/s 就别开批）
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 /tmp/altest/qos.py

# 2) 跑小样对比（质量等价已验证）
tools/al-fast --limit 3 --report /tmp/al-fast-r.json
tools/al-test3 --limit 3

# 3) 逐字段对拍两条流水线
python3 -c "..."   # 见报告第三节脚本
```
