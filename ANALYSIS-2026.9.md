# 2026.9 修图风格逆向 + 不经过 VLM 的快速方案

## 🟩 一句话结论

**我从你 2026.9 目录的 47 张成片里把你的修法逆向出来了：它是一套「固定配方 + 高光按溢出量浮动」的流程，不需要 VLM。**
做成 `tools/al-recipe` 后，**0.23 s/张**（对比 VLM 四道工序 ~190 s/张，**快约 800×**），
且逐键对拍你的成片：Highlights 平均差 2.0、Contrast 0.7、Blacks 0.7、Whites 0.75、Vibrance 1.67。

---

## 一、你的修法（从成片逆向，按工序）

判定依据：47 张成片的 develop 记录统计（已剔除微信图片/异常样本）。

| # | 工序 | 实测使用率 | 参数（中位） |
|---|---|---|---|
| 1 | 启用镜头配置文件 | 92–100% | `LensProfileEnable=1` |
| 1 | 删除色差 | 92–100% | `AutoLateralCA=1` |
| 2 | 自动透视 Upright | 69–95% | `PerspectiveUpright=1` |
| 3 | 裁剪 | 31% | `CropTop≈0.006` |
| 4 | 相机配置文件 | 100% | JPG→`Embedded`／RAW→**`Adobe Standard`** |
| 4 | 点曲线 | 47% | **RAW 专属 S 曲线**（21/21 完全相同） |
| 5 | 曝光 | 100% | JPG −0.12／RAW +0.33 |
| 5 | 对比度 | 100% | **7** |
| 5 | 高光 | 96% | JPG −8.5／RAW −64 |
| 5 | 阴影 | 100% | JPG 29／RAW 49 |
| 5 | 白色色阶 | 98% | 10.5 |
| 5 | 黑色色阶 | 100% | **−15** |
| 6 | 鲜艳度 | 100% | **14–15** |
| 6 | 饱和度 | 69% | 1 |
| 7 | 清晰度/去朦胧 | 2%/7% | 基本不用 |
| 8 | 锐化 | 49% | 40 |
| 8 | 颜色降噪 | 47% | 25 |
| 9 | 色温/色调 | 47% | 仅 RAW（5400K / +9） |
| 10 | **AI 皮肤蒙版** | **91%** | 面部皮肤 + 身体皮肤 |

### 关键发现：RAW 与 JPG 是两条分支

**决定性变量 = 相机配置文件**（不是文件扩展名）：

| | RAW (.dng) | JPG |
|---|---|---|
| CameraProfile | **Adobe Standard** | Embedded |
| 点曲线 | **S 曲线**（0,0,22,16,40,35,127,127,224,230,240,246,255,255） | 线性 |
| Highlights 中位 | **−64**（几乎恒定） | **−8.5**（按溢出量浮动，−71~0） |
| Exposure 中位 | **+0.33**（RAW 欠曝，要提亮） | −0.12 |
| Vibrance | 恒定 **15** | 14（10~20） |
| Shadows 中位 | 49 | 29 |

> 你之前提醒「我做了很多修改而不只是自动」——对。我一开始被 `AutoToneDigest`（45/45 出现）
> 误导以为是自动色调，**那个判断是错的**：`Brightness=50/Contrast=25/Shadows=5` 只是
> PV2010 旧键的默认残留，真正生效的是现代键 2012 系列，且分布明显是人工选择
> （Contrast 恒 7、Blacks 恒 −15、RAW/JPG 分派清晰），不是 Auto Tone 的输出。

---

## 二、验证（诚实的误差表）

**验证集问题**：catalog 里 49 张，**41 张原片已不在磁盘**（在 `/Volumes/CANON 600D` 与外部位置）。
可用真原片只有 4 张（`~/Pictures/2026/2026-09-23/`）。我用这 4 张做**前向验证**：

| 参数 | 用什么规则 | 常量基线 MAE | 规则 MAE |
|---|---|---|---|
| Highlights2012 | 溢出 >2.5% → −55，否则 −1 | 15.75 | **1.50** ✅ 10× 提升 |
| Contrast2012 | 常量 7 | — | 0.75 |
| Blacks2012 | 常量 −15 | — | 1.00 |
| Whites2012 | 常量 10.5 | — | 0.75 |
| Vibrance | 常量 14 | — | 1.50 |
| Exposure2012 | 常量 −0.12 | — | 0.11 |
| **Shadows2012** | 常量 29 | — | **11.75** ⚠️ |

### ⚠️ 诚实的边界：Shadows 推不动

`Shadows2012` 与所有可用测量的相关系数都很低（JPG 最佳 +0.37，RAW +0.25）。
**「该提多少阴影」取决于主体/人脸在不在暗部——那是语义判断，数值推不出来。**
所以本工具给中位常量，MAE 约 11.75，**这是本方法的主要误差来源**，不假装它准。
要精确的阴影量，只有 VLM 或人工判断两条路。

### 另一个坑（已量化）

我一度想用 **Smart Previews**（49 个，与照片 1:1 对应）当「原片」测量 ——
因为 41/49 原片离线，这是唯一能覆盖全量的办法。**但实测两者不在同一测量域**：

| 同张照片 | mean | p50 | p95 | 高光溢出% |
|---|---|---|---|---|
| IMG_1415.JPG 原片 | 122.6 | 130.6 | 218.4 | 0.41 |
| 同张 Smart Preview | 149.6 | 177.5 | 245.9 | **2.57** |

Smart Preview 是**已应用相机配置文件/曲线的加工版**，不是原始传感器数据。
所以在它上面拟合出的阈值（12%）**不能**用于真原片 —— 我换成真原片重搜，才得到正确的 **2.5%**。
**结论：Smart Previews 不能当原片测量基准。**

---

## 三、交付：`tools/al-recipe`

```bash
# 1) 从任何"已修目录"学配方（这次学的是 2026.9）
tools/al-recipe fit --catalog ~/Pictures/Lightroom/2026.9/2026.9.lrcat \
                    --style style-2026.9.json

# 2) 只算不写（对拍/审查用）
tools/al-recipe predict --dir ~/Pictures/摄影社/2026.9/0923中秋活动

# 3) 算并写入 Lightroom（自动分 RAW/JPG 分支）
tools/al-recipe apply   --dir ~/Pictures/摄影社/2026.9/0923中秋活动

# 4) 只写不跑（演练）
tools/al-recipe apply   --dir ... --dry
```

**实测吞吐：43 张 / 10.1 s = 0.23 s/张**（纯 al-vision 数值测量，**零 VLM 调用**）。

### 与 VLM 方案对比

| 方案 | 每张耗时 | 质量 |
|---|---|---|
| `al-test3`（VLM 四道工序） | ~190 s（热态） | 逐张语义判断，能处理构图/表情/废片 |
| **`al-recipe`（配方直套）** | **0.23 s** | 配方键与成片 MAE 0.75–2.0；**Shadows 11.75** |

**"不经过 VLM 快速解决"完全成立** —— 前提是你接受 Shadows 这一项回落到中位常量。

### VLM 仍然有价值的场景

配方法**做不了**这些（这些是真正需要语义判断的）：
- 废片排除（虚焦/闭眼/表情）——你 2026.9 里没有排除动作，但真实活动照通常需要；
- Shadows 精确量（主体是否在暗部）；
- 构图问题（主体过小/背景干扰 → 是否裁切）；
- 皮肤蒙版的**选区**：91% 的成片有 AI 皮肤蒙版，但插件白名单目前
  **不含 `MaskGroupBasedCorrections`**，配方法无法自动生成皮肤选区 —— 这是已知缺口。

---

## 四、下一步建议

1. **明天真实活动照**：先跑 `tools/al-recipe apply --dir <新目录> --dry` 看参数是否合理，
   确认后再去掉 `--dry`。0.23 s/张，240 张不到 1 分钟。
2. **补皮肤蒙版**：要复现你 91% 的 AI 皮肤蒙版，需给 `Bridge.lua` 的白名单加
   `MaskGroupBasedCorrections`（结构复杂：嵌套 CorrectionMasks + MaskDigest），
   或改用 LR 的「自动蒙版」UI（但那需要 CUA 点击，且 `allow_foreground` 会抢前台）。
3. **Shadows 若要提准**：对 JPG 组做一个小的 VLM 二分类（"主体是否在暗部"，
   输出 1 个令牌即可判 → 选 29 还是 45），单张成本 <1 s，比全套 24 字段便宜得多。
